/**
 * This package is a BIN-ONLY forwarder (see ./bin.js — guuey#449): the
 * importable surface lives at `@guuey/cli`. This module is deliberately
 * inert so `import "guuey"` neither executes the CLI nor errors.
 */
export {};
