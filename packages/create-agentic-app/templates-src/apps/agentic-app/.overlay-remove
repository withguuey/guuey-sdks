# Files the agentic-app overlay REMOVES from the assembled base (one
# project-relative path per line; '#' comments). The build fails if a listed
# path does not exist — a stale entry is a bug, not a no-op.
#
# The standalone /chat page is superseded by the agent RAIL inside the
# app shell (the chat-rail layout, guuey#303).
web/src/pages/Chat.tsx
